# SWE-Forge reproducible environment pin.
#
# One package manager only (pip). Upstream Open SWE keeps its own uv.lock; this
# file pins the SWE-Forge layer's direct dependencies so an evaluation run is
# attributable to a known dependency set.
#
# Verified environment:
#   Python           3.12.3
#   Upstream Open SWE  git commit recorded per-run in evaluation manifests
#
# Install:  pip install -r requirements-sweforge.txt
#
# Ranges (not exact pins) are deliberate: SWE-Forge must install into an existing
# Open SWE checkout without forcing a downgrade of upstream's own pins.

# --- core runtime (required) -------------------------------------------------
langgraph>=1.2.10,<2
langchain>=1.3.9,<2
langchain-core>=1.6.0,<2
pydantic>=2.9,<3
langsmith>=0.11.1,<1

# --- development and quality gates (no credentials needed) ------------------
pytest>=8
ruff>=0.6
mypy>=2.3.1

# --- OPTIONAL: Open SWE baseline adapter (Experiment B) ---------------------
# Only needed to import the real upstream agent factory.
# fastapi
# deepagents==0.7.6

# --- OPTIONAL: live-model evaluation (Experiment C) -------------------------
# langchain-anthropic
# langchain-openai
